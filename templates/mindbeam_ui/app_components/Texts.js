import React from "react";
import {HelperText} from "react-native-paper";
import styles from './styles';


export default (styles) => (<HelperText type="error" style={styles.errorText}>

</HelperText>);
export default (styles) => (<HelperText type"info" style={styles.successText}>

</HelperText>);
